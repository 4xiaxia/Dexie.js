interface:
  display_name: "Dexie.js"
  short_description: "Repo-specific patterns and workflows for Dexie.js"
  default_prompt: "Use the Dexie.js repo skill to follow existing architecture, testing, and workflow conventions."
policy:
  allow_implicit_invocation: true